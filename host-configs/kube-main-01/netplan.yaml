# Referência do /etc/netplan/dns-config.yaml do kube-main-01 (cabo, IP estático).
# O Wi-Fi foi desativado; o 50-cloud-init.yaml é removido por fix-netplan-cloud-init.yml.
network:
  version: 2
  ethernets:
    enp0s31f6:
      dhcp4: no
      addresses:
        - 192.168.10.103/24
      gateway4: 192.168.10.1
      nameservers:
        addresses:
          - 192.168.10.2
          - 1.1.1.1
