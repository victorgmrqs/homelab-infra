---
ansible_user: victor
ansible_port: 2816
ansible_ssh_private_key_file: ~/.ssh/id_homelab
ansible_python_interpreter: /usr/bin/python3

# Chave pública autorizada em todos os nós (conteúdo de ../../../../key)
homelab_ssh_pubkey: "{{ lookup('file', playbook_dir + '/../../key') }}"
