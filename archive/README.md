# Archive

Configurações e arquivos de backup que não fazem parte da configuração ativa do homelab.

## laptop-backup

Backup de configurações de outro ambiente (rede 192.168.0.x), recuperado após formatação do laptop. Mantido apenas como referência; a configuração ativa do cluster está em `ansible/` e `host-configs/`.
